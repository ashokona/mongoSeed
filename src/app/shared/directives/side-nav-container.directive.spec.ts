import { SideNavContainerDirective } from './side-nav-container.directive';

describe('SideNavContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new SideNavContainerDirective();
    expect(directive).toBeTruthy();
  });
});
